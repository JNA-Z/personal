/**
 * @file model.h
 * @brief 硬件对象模型层
 * @note 定义和导出所有硬件对象
 *       所有硬件配置在此层静态初始化，BSP层只关注逻辑
 */

#ifndef __MODEL_H
#define __MODEL_H

#include "bsp_led.h"
#include "bsp_key.h"
#include "bsp_encoder.h"
#include "bsp_motor.h"
#include "bsp_adc_photo.h"

#ifdef __cplusplus
extern "C" {
#endif

/* ============================================================================
 * LED对象
 * ============================================================================ */
extern bsp_led_t led1;      /* LED1 */
extern bsp_led_t led2;      /* LED2 */

/* ============================================================================
 * 按键对象
 * ============================================================================ */
extern bsp_key_t key1;      /* 按键1 */
extern bsp_key_t key2;      /* 按键2 */
extern bsp_key_t key3;      /* 按键3 */
extern bsp_key_t key4;      /* 按键4 */

/* ============================================================================
 * 编码器对象
 * ============================================================================ */
extern bsp_encoder_t encoder_left;   /* 左编码器 */
extern bsp_encoder_t encoder_right;  /* 右编码器 */

/* ============================================================================
 * 电机对象
 * ============================================================================ */
extern bsp_motor_t motor_left;       /* 左电机 */
extern bsp_motor_t motor_right;      /* 右电机 */

/* ============================================================================
 * ADC对象
 * ============================================================================ */
extern bsp_adc_photo_t adc_photo;    /* 光敏二极管ADC */

/* ============================================================================
 * 按键扫描接口
 * ============================================================================ */

/**
 * @brief 扫描所有按键
 * @return 按键编号 (1-4), 无按键返回0
 */
uint8_t model_scan_keys(void);

/**
 * @brief 初始化所有硬件对象
 */
void model_init(void);

#ifdef __cplusplus
}
#endif

#endif /* __MODEL_H */
